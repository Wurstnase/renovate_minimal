[tool.poetry]
name = "foo"
authors = []
description = ""
version = "0.0.0"

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
boto3-stubs = {extras = ["sns"], version = "^1.20.42"}

